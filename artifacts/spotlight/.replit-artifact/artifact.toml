kind = "mobile"
previewPath = "/"
title = "Spotlight"
version = "1.0.0"
id = "artifacts/spotlight"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 26217

[services.development]
run = "pnpm --filter @workspace/spotlight run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/spotlight", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/spotlight", "run", "serve" ]

[services.env]
PORT = "26217"
BASE_PATH = "/"
